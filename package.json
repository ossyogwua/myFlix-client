{
  "name": "myflix-client",
  "version": "1.0.0",
  "description": "The client-side for an app called myFlix based on server-side code (REST API and database).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
